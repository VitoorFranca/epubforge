{
  "name": "epubforge-monorepo",
  "private": true,
  "workspaces": [
    "core",
    "cli",
    "desktop"
  ],
  "scripts": {
    "build": "npm run build -w core && npm run build -w cli",
    "test": "npm run test -w core",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
